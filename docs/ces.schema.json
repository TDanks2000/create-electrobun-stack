{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "addons": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "api": {
      "enum": ["electrobun-rpc", "none"]
    },
    "appMenu": {
      "enum": ["edit", "none"]
    },
    "appIdentifier": {
      "type": "string"
    },
    "auth": {
      "enum": ["none", "app-lock"]
    },
    "buildEnv": {
      "enum": ["dev", "canary", "stable"]
    },
    "buildTargets": {
      "enum": ["current", "all"]
    },
    "createdAt": {
      "format": "date-time",
      "type": "string"
    },
    "database": {
      "enum": ["none", "sqlite", "json-file"]
    },
    "dbSetup": {
      "enum": ["none", "seed"]
    },
    "examples": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "features": {
      "additionalProperties": {
        "type": "boolean"
      },
      "type": "object"
    },
    "frontend": {
      "items": {
        "enum": ["react", "preact"]
      },
      "type": "array"
    },
    "git": {
      "type": "boolean"
    },
    "install": {
      "type": "boolean"
    },
    "nativeUtils": {
      "enum": ["none", "file-dialogs", "clipboard", "desktop-kit"]
    },
    "orm": {
      "enum": ["none", "drizzle"]
    },
    "packageManager": {
      "enum": ["bun", "npm", "pnpm", "yarn"]
    },
    "packageName": {
      "type": "string"
    },
    "projectName": {
      "type": "string"
    },
    "query": {
      "enum": ["none", "tanstack-query"]
    },
    "reproducibleCommand": {
      "type": "string"
    },
    "router": {
      "enum": ["none", "tanstack-router", "react-router"]
    },
    "runtime": {
      "enum": ["bun"]
    },
    "settings": {
      "enum": ["none", "json", "database"]
    },
    "styling": {
      "enum": ["tailwindcss", "css"]
    },
    "template": {
      "enum": ["minimal", "standard", "full"]
    },
    "testing": {
      "enum": ["bun", "desktop-smoke", "none"]
    },
    "ui": {
      "enum": ["none", "shadcn"]
    },
    "navigation": {
      "enum": ["local-only", "none"]
    },
    "windowStyle": {
      "enum": ["native", "hidden-inset"]
    },
    "version": {
      "type": "string"
    }
  },
  "required": [
    "$schema",
    "version",
    "createdAt",
    "reproducibleCommand",
    "projectName",
    "packageName",
    "appIdentifier",
    "template",
    "database",
    "orm",
    "runtime",
    "query",
    "router",
    "settings",
    "buildEnv",
    "buildTargets",
    "frontend",
    "styling",
    "ui",
    "appMenu",
    "addons",
    "examples",
    "auth",
    "packageManager",
    "dbSetup",
    "api",
    "navigation",
    "nativeUtils",
    "windowStyle",
    "testing",
    "git",
    "install",
    "features"
  ],
  "title": "create-electrobun-stack manifest",
  "type": "object"
}
